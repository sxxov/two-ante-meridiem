{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "shopify: dev",
			"command": "set -a && source .env && set +a && shopify theme dev",
			"group": "build",
			"runOptions": {
				"runOn": "folderOpen"
			}
		},
		{
			"type": "shell",
			"label": "shopify: push",
			"group": "build",
			"command": "set -a && source .env && set +a && shopify theme push"
		}
	]
}
